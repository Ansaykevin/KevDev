document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.test-btn-main');
    const menuSection = document.querySelector('.mobile');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    
    }
});

document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.light-button');
    const menuSection = document.querySelector('.body-test');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    
    }
});
document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.about-mobile-main');
    const menuSection = document.querySelector('.about-mobile');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    }
});
document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.service-mobile-main');
    const menuSection = document.querySelector('.service-mobile');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    }
});
document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.work-mobile-main');
    const menuSection = document.querySelector('.work-mobile');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    }
});
document.addEventListener('DOMContentLoaded', function () {
    
    const menuIcon = document.querySelector('.contact-mobile-main');
    const menuSection = document.querySelector('.contact-mobile');

    if (menuIcon && menuSection) {
        menuIcon.addEventListener('click', function () {

            menuSection.classList.toggle('active');
         
        });
    }
});



